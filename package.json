{
  "name": "claude-nebula-dev",
  "version": "1.0.0",
  "description": "Build tools for claude-nebula plugin - Semantic memory powered by Nebula AI",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node plugin/scripts/build.js",
    "clean": "rm -rf plugin/scripts/*.cjs",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@nebula-ai/sdk": "^1.0.0"
  }
}
