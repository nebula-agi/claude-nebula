{
  "_comment": "MCP integration is optional. This plugin uses the Nebula SDK directly. To enable MCP server, remove underscore prefix from _mcpServers.",
  "_mcpServers": {
    "nebula-memory": {
      "type": "http",
      "url": "https://mcp.trynebula.ai/mcp",
      "headers": {
        "Authorization": "Bearer ${NEBULA_API_KEY}"
      }
    }
  }
}
